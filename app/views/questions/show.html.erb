<%= @question.title %>
<br>
<%= @question.body %>
<% @question.answers.each do|answer| %>
  <br>
  <%= answer.votes.count %> <%= button_to "Vote", answer_votes_path(answer, user_id: current_user, answer_id: answer.id) %> <%= answer.body %>
  <br>
<% end %>

<%= render partial: 'answers/inline_form' %>
